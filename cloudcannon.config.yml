collections_config:
  api:
    schemas:
      default:
        path: _schemas/api.md
  documentation:
    schemas:
      default:
        path: _schemas/documentation.md

_inputs:
  type:
    type: select
    options:
      values:
        - get
        - put
        - delete
        - post